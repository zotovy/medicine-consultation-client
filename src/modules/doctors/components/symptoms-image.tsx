"use strict";

import React from "react";

const SymptomsImage: React.FC = () => {
	// return(
	// 	const TabContent = ({ title, sourseSvg}) => (
	// 			<div className="tabcontent">
	// 				<h3>{title}</h3>
	// 				<div>
						
	// 				</div>	
	// 			</div>
	// 		);
	// 		function Tabs({ items:array}):any {
	// 				  const [ active, setActive ] = React.useState(null);

	// 				  const openTab = e => setActive(e.toNumber.target.dataset.index);

	// 				  return (
	// 				    <div>
	// 				      <div className="tab">
	// 				        {items.map((n, i) => (
	// 				          <button
	// 				            className={`tablinks ${i === active ? 'active' : ''}`}
	// 				            onClick={openTab}
	// 				            data-index={i}
	// 				          >{n.title}</button>
	// 				        ))}
	// 				      </div>
	// 				      {items[active] && <TabContent {...items[active]} />}
	// 				    </div>
	// 				  );
	// 				}

	// 				const items: Array = [
	// 				  { title: 'М', sourseSvg: '/' ,active: true},
	// 				  { title: 'Ж', sourseSvg: '/' ,active: false},
	// 				];
	// )
	
}

export default SymptomsImage;